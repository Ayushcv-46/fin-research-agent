# from agents.analyst_agent import analyst_agent_node  # will build this on Day 14

def analyst_agent_node(state: dict) -> dict:
    """TEMPORARY placeholder so the graph runs end-to-end until Day 14."""
    print("Analyst would run here with chunks:", state.get("retrieved_chunks"))
    return {"report_draft": "PLACEHOLDER — Analyst not built yet (Day 14)"}